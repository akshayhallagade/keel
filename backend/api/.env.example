# DATABASE_URL is not set here — it is read from backend/db/.env, so the API and
# the Prisma CLI cannot drift apart. Set it there.
PORT=4000
JWT_SECRET="change-me"
WEB_ORIGIN="http://localhost:5173"
